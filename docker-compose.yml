version: '3.8'

services:
  backend:
    build: .
    container_name: tms-backend
    ports:
      - "8080:8080"
    restart: always

  frontend:
    build: ./front
    container_name: tms-frontend
    ports:
      - "3000:3000"
    restart: always
    depends_on:
      - backend